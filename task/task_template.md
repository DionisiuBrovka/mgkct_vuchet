# Task NN — <Краткое название задачи>

## Статус

`не начата`

Допустимые статусы: `не начата`, `в работе`, `заблокирована`, `выполнена`.

## Роль задачи в общем плане

Кратко объясни, к какому этапу рефакторинга относится задача и почему она должна выполняться именно в указанной точке плана.

## Контекст

Опиши только тот продуктовый и технический контекст, который необходим новой сессии для выполнения этой задачи. Ссылайся на `task/product_spec.md`, но продублируй критические ограничения, которые исполнитель не должен пропустить.

## Подтверждённое текущее состояние

Перечисли найденные в репозитории проблемы и особенности, на которых основана задача. Для каждого существенного утверждения укажи конкретный путь к файлу, класс, маршрут, миграцию, тест или конфигурацию.

- `<path>` — что найдено и почему это важно.

Не выдавай предположение за факт. Непроверенную информацию явно помечай как предположение или открытый вопрос.

## Зависимости

- `Task NN — <название>` — какой результат предыдущей задачи необходимо использовать.

Если зависимостей нет, напиши `Нет`.

## Условия начала

- [ ] Все обязательные зависимости имеют статус `выполнена`.
- [ ] Необходимые архитектурные решения, схема или API-контракт зафиксированы.
- [ ] Нет открытого вопроса, который блокирует выполнение.
- [ ] Рабочее дерево и результаты предыдущих задач проверены.

## Цель

Одним абзацем опиши конкретное целевое состояние после выполнения задачи.

## Ожидаемый результат

Перечисли наблюдаемые артефакты и изменения, которые должны существовать после выполнения. Не подменяй ожидаемый результат общими формулировками вроде «улучшить код».

## Объём работ

Подробно опиши, что требуется исследовать, изменить, создать или удалить.

1. ...
2. ...
3. ...

## Что не входит в задачу

Явно перечисли соседние изменения и будущие функции, которые исполнитель не должен делать в этой сессии.

- ...

## Затрагиваемые компоненты и файлы

- `path/to/component/` — ожидаемый характер изменений.

Не придумывай файлы как существующие. Для нового файла явно укажи, что его требуется создать. Список может уточняться исполнителем после проверки репозитория, но расширение объёма задачи должно быть обосновано.

## Функциональные требования

- ...

## Архитектурные ограничения

- Клиент не обращается к PocketBase напрямую.
- Окончательные права, валидация и бизнес-правила реализуются сервером.
- Не добавляй обратную совместимость со старой базой и API.
- Не усложняй решение функциями, отсутствующими в `task/product_spec.md`.
- Добавь только содержательные комментарии для неочевидной логики.
- Сохраняй согласованность с результатами выполненных зависимостей.

Оставь в этом разделе только ограничения, относящиеся к конкретной задаче, и добавь необходимые специфические правила.

## Порядок выполнения

Опиши последовательность действий на уровне инженерной работы, но не пиши готовую реализацию или большие фрагменты кода.

1. Сначала проверить ...
2. Затем изменить ...
3. После этого проверить ...

Исполнитель вправе выбрать конкретный способ реализации в рамках требований и должен обосновать значимое отклонение от предложенного порядка.

## Ошибки и особые случаи

- ...

Укажи негативные сценарии, граничные значения, ошибки сети, состояния данных, права и конкурентные изменения, относящиеся к задаче.

## Критерии приёмки

- [ ] Каждый пункт описывает проверяемый результат.
- [ ] ...

Критерии должны проверять поведение и целевое состояние, а не только наличие изменённых файлов.

## Автоматические проверки

Укажи точные актуальные команды, которые необходимо выполнить после изменений:

```bash
# команды конкретной задачи
```

Не требуй полный набор дорогих проверок, если задача затрагивает только изолированную часть. Полный мастер-скрипт обязателен для интеграционных и финальных задач.

## Ручная проверка

Опиши воспроизводимый пользовательский или эксплуатационный сценарий:

1. ...
2. ...
3. Ожидаемый результат: ...

Если ручная проверка не нужна, объясни почему.

## Документация и очистка

Укажи, какие документы, примеры конфигурации или инструкции необходимо обновить. Перечисли временные артефакты, которые следует удалить перед завершением.

## Риски

- Риск: ...
  Мера снижения: ...

## Инструкция исполнителю

Перед началом прочитай этот файл, `task/README.md`, `task/product_spec.md`, актуальный `AGENTS.md` при его наличии и все перечисленные зависимости. Проверь фактическое состояние репозитория: предыдущие задачи могли изменить пути и реализацию.

Выполни только эту задачу и доведи её до проверяемого результата. Не останавливайся на плане или рекомендациях. Не изменяй несвязанный код и не реализуй функции из раздела «Что не входит в задачу».

После реализации:

1. выполни предусмотренные автоматические и ручные проверки;
2. проверь итоговый diff на случайные изменения и временные файлы;
3. обнови статус этой задачи в `task/README.md`;
4. в этом файле добавь раздел `Результат выполнения` с кратким перечнем фактических изменений, выполненных проверок и отклонений от плана;
5. если обнаружен блокирующий вопрос или несовместимость с будущими задачами, зафиксируй это в `task/README.md` и соответствующих ещё не начатых задачах.

## Результат выполнения

Этот раздел заполняет исполнитель после реализации задачи.
